<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/tx
       http://www.springframework.org/schema/tx/spring-tx.xsd
       http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="com.aladdin.demo"/>

    <!--使用dbcp实现的数据源-->
    <bean id="crawlerDataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
        <!--p:driverClassName="com.mysql.jdbc.Driver"-->
        <!--p:url="jdbc:mysql://localhost:3306/zkx"-->
        <!--p:username="root"-->
        <!--p:password="root">-->
        <property name="driverClassName" value="${crawler.jdbc.driver}"/>
        <property name="url" value="${crawler.jdbc.url}"/>
        <property name="username" value="${crawler.jdbc.username}"/>
        <property name="password" value="${crawler.jdbc.password}"/>
    </bean>
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="crawlerDataSource"/>
        <property name="configLocation" value="classpath:mybatis-config.xml"/>
        <property name="mapperLocations" value="classpath*:com/aladdin/demo/crawler/sql/*.xml"/>
    </bean>
    <bean id="baseDao" class="com.aladdin.demo.dao.BaseDao">
        <property name="sqlSessionFactory" ref="sqlSessionFactory"/>
    </bean>
    <!--jdbc模板bean-->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <!--p:dataSource-ref="dataSource">-->
        <property name="dataSource" ref="crawlerDataSource"/>
    </bean>
    <!--配置事务管理器-->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <!--p:dataSource-ref="dataSource">-->
        <property name="dataSource" ref="crawlerDataSource"/>
    </bean>
    <!--通过AOP配置，提供事务增强，让Service包下所有Bean的所有方法拥有事务-->
    <aop:config proxy-target-class="true">
        <aop:pointcut id="serviceMethod" expression=" execution(* com.aladdin..*.service..*(..))"/>
        <aop:advisor pointcut-ref="serviceMethod" advice-ref="txAdvice"/>
    </aop:config>
    <tx:advice id="txAdvice" transaction-manager="transactionManager">
        <tx:attributes>
            <tx:method name="*"/>
        </tx:attributes>
    </tx:advice>

    <bean name="propertyConfigure" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <list>
                <value>classpath:crawler.properties</value>
            </list>
        </property>
    </bean>
</beans>
